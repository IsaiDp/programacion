#include <cs50.h>
#include <stdio.h>
{
    int get_positive
    do
    {
        num= get_int ("How many time do you like to say meow:"); // El oscarin se la rifa
    }
    while(num < 0)
}

//Entry point: Method main nto execute
int maint ( void)
{

        int num = tget_positive_int()
            say_meow(num)
}
